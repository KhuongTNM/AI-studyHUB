spring.application.name=ai-study-hub-backend
server.port=8080

# =====================================================================
# DATABASE CONFIGURATION
# =====================================================================
# [LOCAL] Dùng để test ở máy local — đang ACTIVE
# app.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/AIStudyHub}
# app.datasource.username=${DB_USERNAME:postgres}
# app.datasource.password=${DB_PASSWORD:12345}

# [SUPABASE]
app.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
app.datasource.username=postgres.qpwnqtvxthaybzqxjwnm
app.datasource.password=@MinhKhuong123
# =====================================================================

spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

app.jwt.secret=${JWT_SECRET:AIStudyHubDevSecretKeyMustBeAtLeast32CharactersLong}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.auth.max-login-attempts=5
NEXT_PUBLIC_GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:207590091116-dhri27e8ejk82ap2meotqba378r03ssh.apps.googleusercontent.com}
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

app.upload.dir=${UPLOAD_DIR:./uploads}

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=55MB

# Resend Email Configuration
resend.api-key=${RESEND_API_KEY:re_MQPwtyR1_MWWmFFqzMafuV4ygzi7KDGHX}
resend.from-email=${RESEND_FROM_EMAIL:onboarding@resend.dev}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:3000}

# OpenAI Embedding API
openai.api-key=${OPENAI_API_KEY:sk-your-key-here}

# =====================================================================
# Thêm đoạn này vào application.properties (hoặc .env tương ứng)
# =====================================================================

# PayOS credentials — lấy từ dashboard.payos.vn (dùng credentials Sandbox khi dev/test)
payos.client-id=${PAYOS_CLIENT_ID:313e57df-ba65-4e3d-9aea-a319cf47dc52}
payos.api-key=${PAYOS_API_KEY:4434c26b-d1d5-4e4d-8cae-276bcb4d69e0}
payos.checksum-key=${PAYOS_CHECKSUM_KEY:c0a10567c4d1837f972c1360397e4335f55820d96fd39378aee6dd37474d8e28}

# URL redirect sau khi thanh toán xong/huỷ (trang frontend)
payos.return-url=${PAYOS_RETURN_URL:}
payos.cancel-url=${PAYOS_CANCEL_URL:}

# AI Service Configuration
ai.service.url=${AI_SERVICE_URL:http://localhost:8000}
ai.service.connect-timeout=5000
ai.service.read-timeout=60000
